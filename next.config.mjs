/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com'], // Standard domain for high-quality placeholder images
  },
};

export default nextConfig;
